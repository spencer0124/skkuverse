---
title: Docs Index and Conventions
type: reference
status: accepted
owner: zoyoong124@gmail.com
last-updated: 2026-08-05
audience: public
---

# Docs index and conventions

> The index and the writing rules for the SKKUverse documentation hub. This repository
> holds cross-cutting knowledge only, and anything local to one repo belongs in that
> repo's own `docs/`.

## What belongs here

| Belongs here (cross-repo) | Does not (repo-local) |
| --- | --- |
| System boundaries and architecture diagrams | One repo's build or deploy runbook |
| Data flows that cross repo lines, such as the notice pipeline | The detailed schema of one collection, which goes to the owning repo |
| Ownership maps: who writes what | Framework-specific detail |
| ADRs whose consequences cross repos | ADRs that begin and end in one repo |

Never copy a schema or a number into this repository. This is an index and a map, so link
to the owning repo's document for detail. [Data Topology](architecture/data-topology.md) is
that map for runtime data, and [`../contracts/manifest.json`](../contracts/manifest.json)
is the machine-readable one for configuration.

## Folder structure

Adopted from skkuverse-app's convention as the workspace standard. Documents are filed by
the reader's need rather than by topic, following [Diátaxis](https://diataxis.fr).

| Folder | Reader's need | Contents |
| --- | --- | --- |
| `architecture/` | Explanation, to understand | System boundary, container view, data topology (C4) |
| `flows/` | Explanation, to understand | End-to-end flows that cross repos |
| `how-to/` | How-to, to accomplish something | Task recipes with a stated starting point |
| `decisions/` | ADR | Cross-repo ADRs, named `NNNN-kebab-title.md` |

Some catalogued documents sit outside `docs/` and follow every rule on this page. Each sits
beside the thing it describes, which is what Diátaxis asks of reference material.

| Directory | Its document covers |
| --- | --- |
| [`../exported/`](../exported/) | the scripts other repos run, and what changing them costs |
| [`../internal/`](../internal/) | the scripts only this repo runs, and their lack of any promise |
| [`../contracts/`](../contracts/) | the config-contract registry, read by tooling rather than by people |
| [`../conventions/`](../conventions/) | rules that apply to every repository in the ecosystem |

## Document index

Generated by [`../internal/render/docs_index.py`](../internal/render/docs_index.py) from each document's own
frontmatter and summary line, and verified in CI. Do not edit it by hand. Membership is a
rule rather than a list: any Markdown file carrying frontmatter is indexed, so a new
document cannot be left out by forgetting to register it.

<!-- index:start -->
### architecture

| Document | Type | Summary |
| --- | --- | --- |
| [Container View](architecture/container-view.md) | explanation | Inside the system: how the repositories, MongoDB and Firebase interlock (C4 Level 2). One level out is System Context, and this same picture in motion is Notic… |
| [Data Topology](architecture/data-topology.md) | reference | The single map of which repo owns which data and where its schema is documented. This document never duplicates schema fields. It links to the owning repo's ca… |
| [System Context](architecture/system-context.md) | explanation | SKKUverse's system boundary — who uses it, and which parts of the outside world it depends on (C4 Level 1). One level in: Container View. |

### contracts

| Document | Type | Summary |
| --- | --- | --- |
| [Cross-Repo Config Contracts](../contracts/README.md) | reference | Configuration owned by one repo and vendored by another is a *contract*. `manifest.json` is the list of them, and `../exported/sync_contracts.py` is what keeps… |

### conventions

| Document | Type | Summary |
| --- | --- | --- |
| [Shared Conventions](../conventions/README.md) | reference | Rules that apply to every SKKUverse repository, defined once here and enforced in each repo's CI. Conventions that are files travel as contracts. Conventions t… |
| [Branching](../conventions/branching.md) | reference | Commit to `dev`, and reach `main` by pull request. A separate branch is the exception, and this page names the cases that earn one. |
| [Cross-Repository References](../conventions/cross-repo-references.md) | reference | How to link across SKKUverse repositories, and why an unqualified reference resolves to the wrong object rather than failing visibly. |
| [Prose Conventions](../conventions/prose.md) | reference | The writing rules are executable. `../.vale.ini` and `../styles/skkuverse/` are the rules themselves, and CI runs them. This page covers only what a linter can… |

### decisions

| Document | Type | Summary |
| --- | --- | --- |
| [Notice Data Ownership Across Writers](decisions/0001-notice-data-ownership.md) | adr | The crawler, the AI service and the server all touch the `skku_notices.notices` collection. This is the cross-repo contract for what each of them owns, and rep… |
| [Pull-Based Config Contracts with Content Hashes](decisions/0002-pull-based-config-contracts.md) | adr | Configuration owned by one repo and vendored by others used to be pushed across repo boundaries by a script whose failure mode was silence. It is now pulled, p… |
| [Daily Fleet Pin as Git Submodules](decisions/0003-daily-fleet-pin-as-submodules.md) | adr | Every repo's `main` is pinned as a submodule at the repository root once a day and committed, making this repository's history a day-by-day record of what the… |
| [Event Map Layer Ownership Across Repos](decisions/0004-event-map-layer-ownership.md) | adr | A temporary event map is drawn by four repos at once, so this records which of them decides what. The build itself is tracked in skkuverse#11. |
| [Web Surfaces in a Dedicated Repository](decisions/0005-web-surfaces-dedicated-repo.md) | adr | Every browser surface moves out of the React Native monorepo into `skkuverse-web`, because what the web app shares with the native one is a message contract ra… |
| [Console Identity and Authorization](decisions/0006-console-identity.md) | adr | The admin console serves external users who sign themselves up, so identity reuses the ecosystem's existing auth provider and every authorization decision is a… |

### exported

| Document | Type | Summary |
| --- | --- | --- |
| [Exported Scripts](../exported/README.md) | reference | The public interface of this repository. Other repos clone it during CI and run these scripts by absolute path, so anything here is a contract. Everything else… |

### flows

| Document | Type | Summary |
| --- | --- | --- |
| [Notice Pipeline (End-to-End)](flows/notice-pipeline.md) | explanation | The full path of the AI notice feature, from crawling a department site to rendering on screen, as one route across the fleet. The static structure is in Conta… |

### how-to

| Document | Type | Summary |
| --- | --- | --- |
| [Expand a Past Snapshot](how-to/expand-a-past-snapshot.md) | how-to | Recover the state of every SKKUverse repository on a chosen date, and check out the code as it was. Why the snapshot exists at all is ADR 0003. |

### internal

| Document | Type | Summary |
| --- | --- | --- |
| [Internal Scripts](../internal/README.md) | reference | Scripts that only this repository runs. Nothing outside it may depend on them, so they can be renamed or deleted freely. The opposite guarantee to `../exported… |
<!-- index:end -->

The repo-root entry points, [`../README.md`](../README.md) and
[`../CLAUDE.md`](../CLAUDE.md), are exempt from frontmatter and from the index. GitHub
renders frontmatter as a table, which does not belong on a landing page, and neither file
is a catalogued document.

## Writing rules

### 1. Frontmatter (required)

```yaml
---
title: <Title Case>
type: reference | explanation | how-to | adr
status: draft | accepted | superseded | deprecated
owner: zoyoong124@gmail.com
last-updated: YYYY-MM-DD
audience: internal | public
---
```

Required on every catalogued document, including the ones outside `docs/`. Use
`audience: public` for anything published as portfolio and `audience: internal` for
private working notes.

Update `last-updated` in the same commit that changes the body. A stale date is a lie that
costs nothing to tell.

### 2. Skeleton

Frontmatter, then exactly one `# H1`, then a `> one-line summary`, then `##` sections with
no skipped levels. Copy [`_template.md`](_template.md) to start.

The summary line is not decoration. `docs_index.py` reads it to build the index above, so a
document without one fails the build.

### 3. Point at the source, don't copy the value

Never hardcode a version, a count, or a schema field that another repository owns. When
that repo changes, this one starts lying and nothing reports it.

- Wrong: `there are 149 crawl sources`
- Right: `the source list's SSOT is skkuverse-crawler's sources.json`

The same rule covers state, not only values. Per-repo adoption status and contract counts
are generated onto the landing page rather than written there.

### 4. Diagrams

Use Mermaid by default, since GitHub renders it natively with no build step. Reach for
PlantUML or C4 sources in `diagrams/` only when Mermaid genuinely cannot express the shape.

### 5. Language and prose

English everywhere. [`../CLAUDE.md`](../CLAUDE.md) carries the policy and its one carve-out
for Korean product copy, and `exported/lint_conventions.py` enforces it.

Prose style has its own rules in [`../conventions/prose.md`](../conventions/prose.md),
enforced by [`../.vale.ini`](../.vale.ini) and `internal/check/prose.py`.

### 6. Filenames and formatting

Lowercase kebab-case `.md`, with ADRs as `NNNN-kebab-title.md`. Code fences always carry a
language tag. Structured facts go in tables.

## Related

- [Workspace landing page](../README.md)
- [`../CLAUDE.md`](../CLAUDE.md) — working constraints, including the governing rule for
  every check in the ecosystem
- [skkuverse-app docs conventions](https://github.com/spencer0124/skkuverse-app/tree/main/docs)
  — the original standard this adopts
